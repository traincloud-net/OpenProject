apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storage-openproject-config
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ionos-enterprise-ssd  # IONOS storage class